# ==================================
# UniPlateTracker Application Config
# ==================================

# --- PostgreSQL Database ---
# The complete connection string for your database.
POSTGRES_URL="postgresql://YOUR_DB_USER:YOUR_DB_PASSWORD@YOUR_DB_HOST:5432/uniplatetracker"

# --- Webhook Worker Service ---
# The network port for the background worker to listen on for UniFi Protect webhooks.
WORKER_PORT="4000"

# --- Next.js Frontend & API ---
# The canonical URL of your web application.
# For local development, this is typically http://localhost:3000.
# For remote development or production, use your server's IP and port.
NEXTAUTH_URL="http://localhost:3000"

# --- NextAuth.js (for future authentication) ---
# A strong, random secret key used for signing tokens (JWTs, etc.).
# You can generate a strong secret by running this command in your terminal:
# openssl rand -base64 32
NEXTAUTH_SECRET=""